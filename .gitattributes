# Normalizar line endings en el repo (evita churn CRLF/LF entre sistemas).
* text=auto eol=lf

# Los scripts de shell DEBEN quedar con LF o se rompen en Linux/macOS.
*.sh text eol=lf

# PowerShell es cómodo con CRLF en Windows.
*.ps1 text eol=crlf
